# FF14 Chat Overlay 更新日志

> **版本命名说明：** 历史归档中存在少量“文件夹名”与程序内部版本号不一致的过渡构建。为避免混淆，本文保留归档版本，同时注明实际内部版本或后续状态。

## v1.1.1

### 区域检索优化

- 完善区域检索功能，优化区域名称匹配与查询体验。

## v1.1.0

### 发布与兼容性

- 正式统一当前发布版本号为 **v1.1.0**。
- 为 `main.css` 与全部本地 JavaScript 增加 `?v=1.1.0` 缓存键，避免 GitHub Pages 更新后 OverlayPlugin/CEF 继续混用旧 CSS、旧 JS。
- 页面增加 `Cache-Control`、`Pragma`、`Expires` 缓存提示，提高版本更新后的重新加载稳定性。
- 组队记录一级、二级导航按钮增加 `appearance: none` 兼容处理，避免 CEF 回退成白色系统按钮。
- GitHub Pages 发布目录加入 `.nojekyll`，并清理无实际用途的空 `vendor` 目录。
- 保留 v1.0.12 及此前全部聊天、历史、组队、每日遇到、区域名称和顶部分类功能。

## v1.0.12

### 顶部分类 UI 优化

- 优化“设置 → 顶部分类”管理窗口。
- 去除弹窗外层 Chromium 默认白色滚动条，列表使用与主聊天窗口一致的细圆角滚动条。
- 调整分类条目高度、内边距与文字行高，让分类名称和“系统分类 / 自定义分组”等说明完整显示在对应条目框内。
- 优化拖动标记、上下移动、移除按钮的垂直对齐。
- 小高度与窄窗口下仍可正常滚动和操作。

## v1.0.11

### 日期选择与滚动适配

- “聊天日期”窗口增加与主聊天区一致的滚动条；窗口较小时，日历内容可独立滚动，不再被底部裁切。
- 跨时间检索取消 Chromium 原生 `type=date` 控件，避免 ngld/CEF 中原生日历弹出后无法点击的问题。
- 开始日期、结束日期改为插件自绘日期按钮。
- 新增统一风格的自定义日期选择器，支持上月、下月、今天和未来日期禁用。

## v1.0.10

### 区域名称解析

- 新增 FF14 `TerritoryType` 区域 ID → 中文区域名解析层。
- 当 `ChangeZone` 只提供区域 ID 时，优先查询本地区域表，例如 `137` 显示为“东拉诺西亚”，不再显示“区域 #137”。
- 收到 `01` ChangeZone 日志中的真实区域名后自动学习并缓存。
- 副本、讨伐战及后续未内置区域可以通过实际日志逐步补全名称。
- 旧组队记录中的区域 ID 会在显示时重新解析，无需清空历史数据。

## v1.0.9

### 组队地点记录

- 修复玩家详情中“最近组队记录”无法独立向下滚动的问题。
- 最近组队记录使用与主界面一致的滚动条。
- 新增组队相遇地点记录：监听区域变化并保存组队时所在地图或副本。
- 同一次组队跨区域时保留地点路径，例如“萌芽池 → 副本名称”。
- 旧版本无法回溯地点的记录显示“地点未记录”。

## v1.0.8

### 相遇日历与统计显示

- 修复玩家详情“相遇日历”首行日期尺寸异常，全部日期格统一为等大的正方形。
- 聊天日期中的消息数量、每日遇到中的玩家数量固定显示在日期格内部，不再溢出边框。
- 玩家详情弹窗滚动条统一为主聊天窗口的细圆角样式。
- 玩家昵称根据组队次数高亮：
  - 10 次及以上：绿色；
  - 50 次及以上：金色。
- 玩家详情中的昵称同步使用对应等级颜色。

## v1.0.7

### 每日遇到与跨时间检索

- 优化“聊天日期”窗口尺寸，减少正常窗口下不必要的纵向滚动。
- 重构“组队记录 → 每日遇到”页面比例，日历保持紧凑尺寸，不再横向铺满整个悬浮窗。
- 点击某一天后进入独立“当天详情”页面，按当天首次遇到时间从早到晚显示玩家。
- 新增“跨时间检索”：支持开始日期、结束日期和昵称筛选。
- 检索结果按时间顺序展示，每位玩家每天最多保留一条记录。
- 跨时间检索保留“原生输入”，兼容 ngld/CEF 中文输入限制。
- 当天详情和检索结果列表的滚动条与主聊天区统一。

## v1.0.6

### 每日遇到与玩家相遇日历

- 新增“每日遇到”功能。
- 将组队成员和聊天频道说话人按日期汇总，同一玩家同一天只记录一次。
- 每日遇到使用月历展示，并显示当天遇到的玩家数量。
- 点击日期可查看当天玩家及来源：组队 / 聊天。
- 玩家详情新增“相遇日历”：
  - 遇到过或组队过的日期显示为绿色；
  - 无记录日期显示为灰色。
- 历史聊天和旧组队记录参与统计，无需清空旧数据库。
- IndexedDB 增加每日玩家索引，提高后续查询效率。
- 保留团队 A–F 分队前缀解析修复。

## v1.0.5

### 团队 A–F 分队解析

- 修复团队聊天中 A–F 分队前缀无法识别的问题。
- 已确认国服日志 `U+E072` 对应 B 队，并按连续字符范围将 `U+E071～U+E076` 解析为 A～F。
- 原始私有字符会被移除，不再显示为方框。
- 历史记录如果保留 `senderRaw`，会在显示时自动重新解析。

## v1.0.4

### 顶部分类管理重构

- 放弃 v1.0.3 在主聊天顶部增加左右翻页按钮的方案，回到 v1.0.2 的主界面逻辑。
- 将“顶部分类”管理窗口改为二级切换：
  - 顶部已显示；
  - 可添加到顶部。
- 一次只显示一个完整列表，避免两个小滚动区域同时出现。
- 增加已显示 / 可添加分类数量提示。
- 中间列表区域使用单一滚动区域，底部“恢复默认顺序 / 完成”保持固定。

## v1.0.3（试验版，后续方案未沿用）

### 顶部分类横向翻页试验

- 在主聊天顶部分类栏增加左右翻页按钮。
- 分类溢出时显示左右渐隐提示。
- 按当前可视宽度进行分页移动。
- 鼠标滚轮可横向浏览分类。
- 切换分类后自动滚动到可见区域。
- 增加轻量滚动吸附。
- 该方案随后回退，后续改为优化“设置 → 顶部分类”管理界面。

## v1.0.2

### 窗口与滚动条统一

- 二级、三级页面的纵向滚动条统一为主聊天列表样式。
- 左上角新增折叠按钮，可将整个聊天栏收起为一个小图标，再次点击恢复。
- 折叠时关闭临时弹窗，恢复后保持原页面状态。
- 页面指引文字沿用当时定稿的 index 内容。

## v1.0.1（归档发布包）

### GitHub 发布整理

- 主要用于 GitHub Pages 发布目录整理。
- 完善项目介绍、安装说明和 Pages 地址。

## v0.11.1

### 组队记录排序 UI

- 组队记录排序改为二级标签页。
- 支持“最近遇到 / 次数 / 昵称”三种排序方式。
- 移除原下拉排序控件，当前排序通过导航高亮显示。

## v0.11

### 整体体验优化与 GitHub 发布准备

- 设置页重新划分为“外观 / 频道 / 数据”三个区域。
- 新增“恢复默认界面”，恢复显示设置但不删除聊天记录、组队记录或自定义分组。
- 优化聊天视觉层级：时间和服务器信息弱化、频道信息更紧凑、消息悬停效果更轻。
- 优化顶部频道标签：长名称省略、完整名称提示、激活下划线、平滑横向滚动和鼠标滚轮横向导航。
- 日期选择旁增加“前一天 / 后一天”快捷按钮。
- 日历中没有聊天记录的日期降低视觉权重。
- 组队记录增加按最近时间、组队次数、昵称排序，并保存排序偏好。
- 复制、颜色、顶部分类、自定义分组、排序、恢复等操作统一使用轻量 Toast 提示。
- 增加面向 GitHub Pages 的 `README.md`、`PRIVACY.md` 和 `CHANGELOG.md`。

## v0.10.1

### 清理测试功能

- 删除“插入测试消息”按钮。
- 删除对应演示消息生成逻辑。
- 空状态提示改为进入游戏发送聊天即可开始记录。

## v0.10

### 顶部分类管理

- 移除 ngld Overlay 中不可靠的浏览器式 JSON 导出 / 导入按钮。
- 新增“顶部分类”管理。
- 系统分类和自定义分组均可显示、隐藏和排序。
- 可从顶部移除“通讯贝”“新人”等默认分类，之后可以重新添加。
- 新建自定义分组默认加入顶部末尾，也可进一步调整位置或隐藏。
- 顶部至少保留一个分类；隐藏分类不会删除聊天记录或自定义分组。

## v0.9

### 历史聊天按日期查看

- 主聊天窗口改为“单日查看”，默认仅加载今天的消息。
- 搜索仅检索当前选中日期，并继续叠加频道 / 自定义分类筛选。
- 工具栏新增日期按钮，支持“今天 / 昨天”。
- 新增聊天日期月历，每个有记录的日期显示当天消息数量。
- 支持上月、下月、回到今天，未来日期不可选择。
- 历史消息按 IndexedDB `timestamp` 索引直接按日期范围读取，不再为查看单日历史加载最近 10,000 条。
- 查看历史日期时，新的实时消息仍继续保存，但不会打断当前历史浏览。
- 跨过午夜且仍处于今天模式时，自动切换到新的一天。

## v0.8.1

### 数据安全

- 删除“组队记录”页面中的“清空记录”按钮及对应清空功能，降低误删组队历史的风险。

## v0.8

### 中文输入与 UI 美化

- 中文搜索恢复为“一级搜索窗口 + 原生输入”模式，不再使用二级自定义输入窗口。
- “原生输入”直接调用 Chromium / 系统 prompt，作为 ngld/CEF 下搜狗输入法的兼容入口。
- 自定义频道分类名称旁增加“原生输入”按钮，支持中文分类名。
- 统一美化顶部频道栏、搜索工具栏、连接状态、按钮、消息悬停、日期分隔、设置窗口、子窗口和频道分类页面。
- 保留自定义频道分类、频道独立颜色、聊天 / 组队记录、消息复制和玩家详情等功能。

## v0.7.1

### 输入入口与工具栏调整

- 互换工具栏中“中文输入”和“组队记录”的位置。
- 优化系统输入辅助窗口样式。

## v0.7

### 自定义频道分类

- 新增用户自定义频道分类功能。
- 用户可以创建分类名称，并自由勾选需要包含的频道。
- 自定义分类保存后直接出现在顶部标签栏。
- 点击自定义标签时，仅显示该分类所包含的频道。
- 支持例如“跨服贝1+2”“小队+部队”等组合。
- 自定义分类保存在本地设置中。

## v0.6.1

### 消息间距微调

- 延续 v0.6 的数据库、玩家详情、备份和复制功能。
- 将“说话人 / 服务器”与消息正文之间的视觉间距由两格调整为一格。

## v0.6

### 数据结构、玩家详情与复制增强

- IndexedDB 升级至 V3：
  - `messages`：聊天记录；
  - `party_history`：玩家累计组队统计；
  - `party_encounters`：逐次组队时间记录。
- 新增数据备份 / 导入 JSON 功能（后续因 Overlay 下载限制在 v0.10 移除）。
- 通讯贝1～8、跨服贝1～8支持分别设置颜色。
- 玩家名 / 服务器与消息之间增加视觉间距。
- 聊天增加日期分隔线，长消息换行保持正文对齐。
- 点击聊天玩家名或组队记录玩家可打开玩家详情。显示累计组队次数、首次记录、最后组队时间。
- 消息复制增强：支持复制正文、整行、玩家名，并可从右键菜单进入玩家详情。

## v0.5

### 组队记录独立页面

- 组队记录升级为独立页面，并支持返回聊天。
- 记录玩家服务器、组队次数和最后一次组队时间。
- 仅在玩家从“未在当前小队”进入“当前小队”时计数，普通 PartyChanged 刷新不会重复累计。
- 切图时加入约 5 秒防抖，降低队伍短暂为空导致的重复计数。
- 组队记录支持昵称搜索，并提供中文输入后备入口。
- 聊天消息悬停显示“复制”按钮。
- 兼容旧 v0.4 组队记录。

## v0.4

### 初版组队玩家记录

- 通过 OverlayPlugin `PartyChanged` 自动记录曾经组队遇到过的玩家。
- 排除自己，保存玩家名、服务器、首次 / 最后遇到时间。
- 即使玩家从未在聊天频道发言，只要进入小队也可以被记录。
- 服务器后缀取消 `@`，保留小号斜体显示。

## v0.3

### 服务器解析、频道颜色与滚动条

- 支持国服 ACT 中“玩家名+服务器名”无分隔形式，例如“十六夜晴天红玉海”。
- 小队顺位继续统一显示为 ①～⑧，并移除原始私有字符。
- 优化聊天垂直滚动条，更宽、更容易抓取，悬停 / 拖动状态更明显。
- 设置中新增频道消息颜色，可分别调整说话、喊话、呼喊、私聊、小队、团队、部队、通讯贝、跨服贝、新人等频道。
- 频道颜色应用于频道标签和消息正文，玩家名仍使用统一字体色。
- 右上角状态简化为 Overlay 连接状态。

## v0.2.3

### 小队顺位与服务器识别修复

- 修复国服小队顺位字符重复显示，彻底移除原始 `U+E0E1～U+E0E8` 字符，只显示 ①～⑧。
- 旧历史记录优先使用 `senderRaw` 重新解析，无需清空数据库。
- 新增 `PartyChanged` 监听，从小队成员 `worldId` 获取服务器名称。
- 内置国服 World ID → 服务器名映射。
- 玩家服务器后缀保持小字号、斜体显示。

## v0.2.2

### 国服顺位字符、中文输入与颜色窗口

- 兼容国服 ACT `U+E0E1～U+E0E8` 小队顺位字符，统一显示为 ①～⑧。
- 新增“中文输入”辅助窗口。
- 背景色 / 字体色不再使用容易消失的浏览器原生 color picker。
- 新增稳定的独立颜色窗口：预设颜色 + RGB 滑杆 + HEX 输入。

## v0.2.1

### 道具字符与中文搜索修复

- 修复 FF14 道具 / 物品链接私有字符 `U+E0BB` 在普通字体中显示为方框的问题，显示为“◆”。
- 保留原始消息文本，旧记录加载时也会自动转换显示。

## v0.2

### 小队顺位、服务器显示与外观自定义

- FF14 小队顺位私有字符转换为可读的 1～8。
- 支持 `U+E061～U+E068`、`U+E090～U+E097` 等顺位字符。
- 玩家服务器后缀独立排版：更小字号、斜体、弱化颜色。
- 设置中新增背景颜色和字体颜色。
- 保留背景透明度调整，并支持恢复默认配色。
- 旧 v0.1.x 历史聊天可重新解析，无需清空记录。

## v0.1

### 首个可用版本

- 建立基于 ACT + FFXIV_ACT_Plugin + ngld OverlayPlugin 的本地聊天悬浮窗。
- 监听 `LogLine` 并解析 FF14 `00` 型聊天日志。
- 支持小队、团队、部队、私聊、LS、CWLS、公共、新人等频道分类。
- 使用 IndexedDB 在本机保存聊天记录，重启后可恢复。
- 启动时加载最近 10,000 条，数据库最多保留 50,000 条。
- 支持聊天搜索、字体大小、背景透明度和时间显示设置。
- 内置最小 OverlayPlugin 本地桥接，不依赖远程 `common.min.js`。
